package View;

import javafx.scene.input.MouseEvent;
import javafx.scene.input.ScrollEvent;

public interface IControlPlusScroll {
    public void Activate(ScrollEvent mouseEvent,MazeDisplayer mazeDisplayer);
}
